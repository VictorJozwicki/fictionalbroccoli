<?xml version="1.0" encoding="UTF-8"?>
<MasterDetailPage   xmlns="http://xamarin.com/schemas/2014/forms" 
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
                    xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
                    prism:ViewModelLocator.AutowireViewModel="True"
                    x:Class="fictionalbroccoli.Views.AppliMenu"
                    >
    <MasterDetailPage.Master>
        <ContentPage Title="Menu">
            <StackLayout Padding="0,50,0,0">
                <Button Text="Accueil" Command="{Binding CommandGoHome}" />  
                <Label Text="Application" FontSize="Large"/>
                <StackLayout Padding="50, 0, 50, 0">
                    <Button Text="Map" Command="{Binding CommandGoAppMap}" />
                    <Button Text="Enregistrements" Command="{Binding CommandGoAppRegister}" />
                    <Button Text="Nouveau" Command="{Binding CommandGoAppNew}" />
                </StackLayout>
                <Button Text="Bonus" Command="{Binding CommandGoBonus}" />
            </StackLayout>
        </ContentPage>
    </MasterDetailPage.Master>
</MasterDetailPage>